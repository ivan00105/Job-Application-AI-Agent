## Tested with python version 3.12.10

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
starlette==0.38.5  # Note: 0.38.6 also works, but 0.38.5 is specified for consistency
python-multipart==0.0.9

# Database
asyncpg==0.29.0
psycopg2-binary==2.9.9  # Keep for potential sync operations

# Authentication
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.2.0

# AI/ML (will add more as needed)
openai==1.54.0
anthropic==0.39.0
httpx==0.27.0
sentence-transformers==2.2.2

# CV Processing
PyMuPDF==1.23.8  # For PDF text extraction (fitz)
python-docx==1.1.0  # For Word documents (DOCX parsing)
Pillow==10.2.0  # Image processing
playwright>=1.40.0  # For HTML to PDF conversion using Chromium (best quality, Windows-compatible)
# Note: After installing playwright, run: playwright install chromium
weasyprint>=60.0  # For HTML to PDF conversion (requires GTK+ on Windows, optional)
# Note: weasyprint dependencies (pydyf, tinyhtml5, tinycss2, cssselect2, Pyphen, fonttools) are auto-installed
xhtml2pdf>=0.2.13  # Pure Python HTML to PDF converter (Windows-compatible fallback)
# paddleocr==2.7.0  # OCR (future)

# Scraping
python-jobspy>=0.5.0
qdrant-client>=1.7.1

# NLP
spacy>=3.7.0

# Scheduling
apscheduler>=3.10.0

# Data Processing
pandas==2.3.3
numpy==1.26.3
scikit-learn==1.4.2

# Utilities
pydantic>=2.6.1,<3.0.0  # Compatible with FastAPI 0.115.0 (2.6.1 minimum, 2.12.4 tested)
pydantic-settings==2.1.0
python-dotenv==1.0.1
requests==2.31.0